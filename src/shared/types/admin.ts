export enum AdminSortTypes {
	Newest = 'newest',
	Oldest = 'oldest',
	Alphabetical = 'alphabetical',
}

export enum RoleTypes {
	All = 'All',
	SuperAdmin = 'SuperAdmin',
	Admin = 'Admin',
	FrontDesk = 'FrontDesk',
}

export enum StatusTypes {
	Active = 'Active',
	Inactive = 'Inactive',
	Blocked = 'Blocked',
}
